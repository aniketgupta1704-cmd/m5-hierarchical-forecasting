# Full pipeline dependencies (training/notebooks) — NOT needed for the Streamlit app.
# The deployed app uses requirements.txt only (precomputed forecasts, no live models).
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=12.0
scikit-learn>=1.3
lightgbm>=4.0
prophet>=1.1
# deep model was trained locally; see run_deep_local.py
u8darts[torch]>=0.27
matplotlib>=3.7
altair>=5.0
streamlit>=1.30
jupyter